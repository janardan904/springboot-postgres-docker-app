spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url:jdbc: postgresql://postgresdb:5432/sbms
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
